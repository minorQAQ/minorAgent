// boot.js -- 唯一入口模块（index.html 的 <script> 指向本文件并携带缓存版本号）。
//
// 背景：app.js 会被 js/ 下多个模块以 import('../app.js') 动态引用（取导出函数）。
// ES 模块以完整 URL（含查询串）为身份标识——若入口直接加载 /app.js?v=N，
// 动态 import 解析到的 /app.js（无查询串）会被视为另一个模块实例，导致
// app.js 顶层副作用执行两遍：事件监听注册两份，toggle 类按钮一次点击触发
// "开+关"互相抵消（左栏/终端按钮表现为点击无反应）。
//
// 因此把版本参数隔离在"不会被任何其他模块 import"的 boot.js 上，
// app.js 全站统一以无查询串的相对路径解析，保证单实例。
import '../app.js';
